export interface Project {
  title: string;
  descriptionEn: string;
  descriptionEs: string;
  image: string;
  playStoreUrl?: string;
  githubUrl?: string;
  webUrl?: string;
  technologies: string[];
}
